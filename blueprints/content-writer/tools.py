"""Writing tools."""

def generate_title(topic: str) -> str:
    return f"Guide to {topic}"

def check_grammar(text: str) -> str:
    return text

def count_words(text: str) -> int:
    return len(text.split())
